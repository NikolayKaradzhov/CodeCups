spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codecups
    username: postgres
    password: Secret123!
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop